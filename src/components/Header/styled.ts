import styled from 'styled-components';

export const HeaderStyled = styled.header`
  --pd: 3rem;
  --pd-x: 1.5rem;

  top: 0;
  gap: 1rem;
  left: 0;
  width: 100%;
  z-index: 10;
  display: flex;
  position: sticky;
  /* box-shadow: var(--shadow); */
  align-items: center;
  justify-content: space-between;
  /* backdrop-filter: blur(1px); */
  /* background-color: rgba(var(--color-contents), 85%); */
`;
